- [OCTOPUS NEXUS OS Architecture](octopus-os.md) — Full-stack Business OS; frontend in mockup-sandbox, backend in api-server; JWT auth + 19 DB tables; OS Core modules: Event Bus, Task/Queue Manager, Brain (decision maker), Decision Engine (arbitration), Rule Engine (data-defined rules), Agent Manager (registry + run history), AI Provider Manager (real Anthropic client + AgentExecutor wiring), Tool Manager (schema-validated invocation + handler registry), Workflow Engine (multi-step orchestration across all of the above), Scheduler (cron/interval-triggered workflow runs and task enqueues), Notification System (channel delivery + in-app inbox + Event Bus trigger).
